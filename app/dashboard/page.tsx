import { getCurrentUser } from "@/lib/auth";
import { PageIntro, QuickActionCard, StatCard, EmptyState } from "@/components/ui";
export default async function DashboardPage() { const user = await getCurrentUser(); return <><PageIntro eyebrow="Your workspace" title={`Welcome back, ${user?.name ?? "there"}.`} description="Create, shape, and manage voices in one focused workspace. Your activity will appear here as you begin creating." /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Total generations" value="—" detail="Available when generation launches" /><StatCard label="Saved voices" value="—" detail="No voice activity yet" /><StatCard label="Usage" value="—" detail="Tracking arrives in a later phase" /><StatCard label="Recent activity" value="—" detail="Nothing to show yet" /></div><section className="mt-10"><h2 className="text-lg font-semibold text-white">Start creating</h2><p className="mt-1 text-sm text-slate-400">Choose a workspace to prepare your next voice project.</p><div className="mt-4 grid gap-4 lg:grid-cols-3"><QuickActionCard icon="◉" title="Text to Speech" description="Turn your script into expressive audio." href="/dashboard/tts" /><QuickActionCard icon="◌" title="Clone Voice" description="Prepare a recording for a personal voice." href="/dashboard/clone" /><QuickActionCard icon="✦" title="Design Voice" description="Describe the voice you have in mind." href="/dashboard/design" /></div></section><section className="mt-10"><h2 className="mb-4 text-lg font-semibold text-white">Recent activity</h2><EmptyState title="Your activity will appear here" description="Generation history is ready to track your work as voice capabilities are introduced." /></section></>; }
